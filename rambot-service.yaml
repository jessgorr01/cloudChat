apiVersion: v1
kind: Service
metadata:
  name: webui
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30088
  selector:
    app: webui

---

apiVersion: v1 
kind: Service 
metadata:  
  name: database 
spec:  
  ports:    
    - port: 3000      
      protocol: TCP      
      targetPort: 3000  
  selector:    
      app: database
 
---
